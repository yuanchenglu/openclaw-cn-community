# OpenClaw 零门槛使用指南

> 写给完全不懂技术的普通人
> 
> 最后更新：2026年2月

---

## 这是什么？能帮我做什么？

**OpenClaw** 是一个**免费的 AI 私人助手**。

简单来说：你可以通过**微信、WhatsApp、Telegram** 等聊天软件，随时和 AI 对话，让它帮你：

- 📝 **回答问题**：任何你想问的事情
- 📄 **写东西**：邮件、报告、总结、翻译
- 🔍 **搜索信息**：帮你在网上查资料
- 📅 **提醒事项**：设置定时提醒
- 🤖 **自动化任务**：帮你处理重复性工作

**和普通 AI 聊天工具的区别**：
- ✅ 完全**免费开源**
- ✅ 数据**存在你自己电脑上**，不会被泄露
- ✅ 可以**24小时在线**，随时响应
- ✅ 支持**国产 AI 模型**（如 DeepSeek、通义千问）

---

## 我需要什么？

### 必须有的
1. **一台电脑**（Windows、Mac 或 Linux 都可以）
2. **网络连接**
3. **一个聊天软件账号**（微信/WhatsApp/Telegram 任选一个）

### 可能需要的（取决于你的选择）
- 如果用 WhatsApp：需要一个**手机号**（建议用一个专门的号码，不要用你的主号）
- 如果用 Telegram：需要**翻墙**才能访问

---

## 第一部分：10 分钟快速上手

### 第 1 步：安装软件

**Mac 用户**：
1. 打开"终端"（在应用程序 → 实用工具 里面）
2. 复制下面这行命令，粘贴进去，按回车：
```
curl -fsSL https://clawd.org.cn/install.sh | bash
```
3. 等待安装完成（大约 1-2 分钟）

**Windows 用户**：
1. 按 Win 键，输入 "PowerShell"，右键选择"以管理员身份运行"
2. 复制下面这行命令，粘贴进去，按回车：
```
iwr -useb https://clawd.org.cn/install.ps1 | iex
```
3. 等待安装完成

> 💡 **遇到问题？**
> - 如果提示"无法识别的命令"：你的电脑可能没有安装 Node.js，请先去 https://nodejs.org 下载安装
> - 如果下载很慢：可能是网络问题，换个时间再试

---

### 第 2 步：运行设置向导

安装完成后，在同一个窗口输入：
```
openclaw-cn onboard
```

向导会问你几个问题：
1. **选择 AI 模型**：推荐选择"DeepSeek"或"通义千问"（国内可用，速度快）
2. **选择聊天软件**：选你常用的那个
3. **其他选项**：一路按回车选默认即可

---

### 第 3 步：连接你的聊天软件

以 **WhatsApp** 为例：

1. 在电脑上输入：
```
openclaw-cn channels login
```
2. 屏幕上会显示一个**二维码**
3. 打开手机上的 WhatsApp → 设置 → 已连接的设备 → 扫描二维码
4. 扫描成功后，连接就建立好了！

---

### 第 4 步：启动服务

输入：
```
openclaw-cn gateway
```

看到 "Gateway started" 就表示成功了！

> ⚠️ **重要提醒**：这个窗口要保持打开，关掉就停止服务了

---

### 第 5 步：开始使用！

现在，用你的手机给助手发一条消息试试：

> 你好，请介绍一下你自己

如果一切正常，你会收到 AI 的回复！

---

## 常见问题

### Q：发消息没有回复怎么办？

1. 检查电脑上的窗口是不是还开着
2. 检查网络连接是否正常
3. 试着重新运行 `openclaw-cn gateway`

### Q：想让它 24 小时在线怎么办？

需要让程序在后台运行。输入：
```
openclaw-cn onboard --install-daemon
```
这样即使关闭窗口，服务也会继续运行。

### Q：怎么更换 AI 模型？

输入：
```
openclaw-cn configure
```
然后选择"模型设置"，可以切换不同的 AI。

### Q：怎么停止服务？

- 如果是前台运行：直接关闭窗口或按 Ctrl+C
- 如果是后台运行：输入 `openclaw-cn gateway stop`

---

## 第二部分：用得更好

### 让 AI 记住你的偏好

你可以在 `~/clawd/SOUL.md` 文件中写入对 AI 的要求，比如：

```
你是我的私人助手，请记住：
- 我喜欢简洁的回答
- 回复时使用中文
- 称呼我为"老板"
```

### 让 AI 主动提醒你

设置定时任务，比如每天早上 9 点发送天气预报：

在配置文件中添加：
```json
{
  "cron": {
    "jobs": [
      {
        "schedule": "0 9 * * *",
        "message": "请告诉我今天的天气"
      }
    ]
  }
}
```

### 在群聊中使用

如果想在群里使用 AI，需要@它才会回复：

1. 把 AI 拉进群
2. 发消息时@它的名字，比如：`@助手 帮我翻译一下这段话`

---

## 术语对照表

如果你在文档中看到这些技术词汇，这里是它们的意思：

| 技术词汇 | 通俗解释 |
|---------|---------|
| CLI | 命令行工具，就是你输入命令的那个黑色窗口 |
| 网关 (Gateway) | 中转站，负责接收和转发消息 |
| 守护进程 (Daemon) | 后台程序，关掉窗口也能继续运行 |
| 令牌 (Token) | 密码/通行证，证明你有权限使用 |
| API | 接口，不同程序之间沟通的方式 |
| 配对 (Pairing) | 建立连接，让两个东西能互相通信 |
| 工作区 (Workspace) | AI 存放记忆和设置的文件夹 |
| 会话 (Session) | 一次对话，从开始到结束 |

---

## 获取帮助

- 📱 **微信群**：扫码加入用户群（见官网）
- 💬 **在线文档**：https://clawd.org.cn
- 🐛 **报告问题**：https://github.com/jiulingyun/openclaw-cn/issues

---

## 安全提醒

1. **不要把 API 密钥告诉别人** - 这相当于你的密码
2. **定期检查对话记录** - 确保没有异常
3. **使用专用号码** - 不要用你的主手机号连接 AI
4. **设置白名单** - 只允许你自己的号码发消息给 AI

---

> 💡 **有任何建议或问题？**
> 欢迎加入微信群反馈！
